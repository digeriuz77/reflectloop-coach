Refinement task:

The coach has reviewed a previous version of this preparation output and reacted to it. Revise the preparation so it responds to the coach's reaction while obeying every rule above, keeping the same required JSON shape, and staying grounded in the original inputs.

Refinement rules:
- Treat the coach's reaction as expert steering. Correct anything they say is wrong, deepen what they ask to deepen, and remove or replace what they say is off-target.
- Re-ground all revised content in the supplied inputs, strategy bank, and rubrics. Do not invent new evidence the coach did not provide.
- Keep sections the reaction does not touch substantively stable, refining wording only where it improves the preparation.
- The full output must remain coach-only, non-evaluative, and anchored in effect on students.
- Return valid JSON only, using the same required JSON shape. Do not include markdown or commentary outside JSON.

Previous output JSON:
{{ previous_output }}

Coach reaction:
{{ coach_reaction }}
